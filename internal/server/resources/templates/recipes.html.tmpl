{{define "title"}}
    Recipes
{{end}}


{{define "content"}}
    <section>
        <table>
        {{range $index, $element := .}}
            <tr>
                <td>{{$element.Name}}</td>
            </tr>
        {{end}}
        </table>
    </section>
{{end}}

