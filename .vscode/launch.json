{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/main.go",
            "args": [
                "server",
                "-c",
                "${workspaceFolder}/config.json",
                "-d",
                "${workspaceFolder}/data.db"
            ],
        }
    ]
}